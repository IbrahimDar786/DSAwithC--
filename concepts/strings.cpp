/*
length() / size()
substr()
find()
append()
insert()
erase()
replace()
empty()
push_back()
pop_back()
compare()
at()
*/

#include <iostream>
using namespace std;

int main() {
  string str = " Hello   ";
  // cout << str.substr(2);
  cout << str.back();
  while (!str.empty() && str.back() == ' ') {
    str.pop_back();
  }

 int left = 0;
 int right = str.length()-1;
 while(left <)


  cout << str;

  return 0;
}